// src/number-validation/typing-direction.ts

/**
 * An enum representing the direction in which the user is currently typing.
 */
export enum TypingDirection {
    Unknown,
    Forward,
    Backward
}
